# 09‑cd-deployment.hlx
cd {
    provider    = "gitlab"
    environment = "production"
}
release {
    tag_prefix = "v"
    changelog  = "CHANGELOG.md"
    assets     = ["target/release/hlx","target/release/hlx.exe"]
}
deploy {
    strategy = "blue‑green"
    script   = "./scripts/deploy.sh"
}